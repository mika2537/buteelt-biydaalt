# .github/workflows/maven.yml
name: Java CI with Maven

on: [push, pull_request]

jobs:
  build:
 the correct path. For example, if `pom.xml`    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
 is in the `project` directory, update the `maven.yml        uses: actions/checkout@v2

      - name: Set up J` file as follows:

```yaml
name: Java CI with Maven

DK 11
        uses: actions/setup-java@v2
        with:
on:
  push:
    branches: [ "main" ]
  pull_request          java-version: '11'

      - name: Build:
    branches: [ "main" ]

jobs:
  build:
    runs with Maven
        run: mvn -f project/pom.xml clean install
-on``:`

 ubuntuMake-l sureatest to

 replace    ` stepsproject:
/p   om - name: Check.xml` with the correct path to your `pom.xml` file.

 out code
      uses: actions/checkout@v4
    -4. **Commit the Changes**: Commit and push the updated workflow name: Set up JDK 23
      uses: actions/setup-java@v configuration to your repository.

By ensuring the correct path4 to
 the `pom.xml` file, the workflow should be      with:
        java-version: '23'
        distribution: able to locate and use it, resolving the issue. 'temurin'
        cache: maven
    -
